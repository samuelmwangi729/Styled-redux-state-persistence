name: first workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps:
      - name: print-hello
        run: echo "hello there"
      - name : print "bye bye"
        run : echo "bye bye"
